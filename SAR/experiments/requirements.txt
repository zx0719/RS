# Core
rasterio>=1.3.0
python-docx>=1.1.0
requests>=2.31.0
numpy>=1.24.0

# Detection
ultralytics>=8.0.0

# LLM inference (local)
transformers>=4.40.0
accelerate>=0.27.0
# torch is installed separately via setup_env.sh (CUDA variant)
# torch>=2.1.0

# LLM inference (API)
openai>=1.0.0

# Image fallback (no geo libs required)
Pillow>=10.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
